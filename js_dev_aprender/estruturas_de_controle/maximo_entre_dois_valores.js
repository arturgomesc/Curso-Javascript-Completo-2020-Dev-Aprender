// Escreva uma função que usa 2 números e retorna o maior entre eles 

let valores = (1, 2);
console.log(valores);

function max(num1, num2) {
    if (num1 > num2) 
        return num1;
    else  return num2; 
}